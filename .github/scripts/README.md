# Github Scripts

If you are looking to add or improve the workflow scripts. Let's setup your environment.

## Setup

1. Move your current working directory to this directory, `cd .github/scripts` 
2. Run: `python -m venv .venv`
3. Run: `.venv`